[project]
name = "ai-scripts"
version = "0.1.0"
description = "AI-based application utility scripts"
requires-python = ">=3.11"
dependencies = []

[tool.uv.sources]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
